% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weighted_qq_plot.R
\name{make_weighted_qq_plot}
\alias{make_weighted_qq_plot}
\title{Make a weighted p-value QQ plot}
\usage{
make_weighted_qq_plot(
  weighted_p_vals,
  weights,
  point_col = "black",
  B = 10000L,
  n_conf_windows = 500L
)
}
\arguments{
\item{weighted_p_vals}{Weighted p-values to plot. Values above 1 are clipped
at 1.}

\item{weights}{Fixed positive weights defining the weighted null
distribution. These are typically normalized to have mean 1.}

\item{point_col}{Point color.}

\item{B}{Number of Monte Carlo samples used to construct the confidence band.}

\item{n_conf_windows}{Number of smallest-p-value ranks included in the
confidence band.}
}
\value{
A \code{ggplot} object.
}
\description{
Compares observed weighted p-values with their theoretical null quantiles,
conditional on the supplied weights.
}
\details{
For fixed weights \code{weights}, let \code{P_i} be independent Uniform(0, 1) null
p-values and let \code{Q_i = min(P_i / weights_i, 1)}. For \code{t < 1}, the average
conditional null CDF of the weighted p-values is
\code{F(t) = mean(pmin(weights * t, 1))}, with \code{F(1) = 1}. The plot compares the
ordered observed weighted p-values with \code{F^-1(r / m)} for ranks 1 through
\code{m}.

Observed weighted p-values are clipped at 1 before ordering, and points equal
to 1 are omitted from the plot. The grey ribbon is a pointwise 95 percent
Monte Carlo confidence band for the smallest \code{n_conf_windows} order
statistics. It conditions on the supplied weights and assumes independent
Uniform(0, 1) input p-values.
}
\examples{
set.seed(4)
w <- sample(x = c(1, 0.5, 0.1, 0.01), replace = TRUE, size = 5000)
w_tilde <- w / mean(w)
weighted_p_vals <- runif(n = 5000, min = 0, max = 1) / w_tilde
qq_plot <- make_weighted_qq_plot(weighted_p_vals, w_tilde, B = 100L)
}
