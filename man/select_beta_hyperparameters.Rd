% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_amplicon_seq.R
\name{select_beta_hyperparameters}
\alias{select_beta_hyperparameters}
\title{Solve for Beta hyperparameters from a target mean and variance}
\usage{
select_beta_hyperparameters(mean, variance)
}
\arguments{
\item{mean}{Target mean of the Beta distribution. Must lie strictly between 0 and 1.}

\item{variance}{Target variance of the Beta distribution. Must be positive and strictly smaller than \code{mean * (1 - mean)}.}
}
\value{
A named numeric vector with entries \code{alpha} and \code{beta}.
}
\description{
Given a desired mean and variance under the Beta parameterization
\code{mean = a / (a + b)} and
\code{variance = ab / ((a + b)^2 (a + b + 1))},
solve for the corresponding positive shape parameters.
}
\examples{
params <- select_beta_hyperparameters(mean = 0.01, variance = 1e-4)
p <- make_beta_prior_density_plot(parameter = "pi", alpha = params[["alpha"]], beta = params[["beta"]], xmin = 0, xmax = 0.1)
}
